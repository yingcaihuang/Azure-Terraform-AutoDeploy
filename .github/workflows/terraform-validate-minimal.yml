name: Terraform Validate - DNS Verification

on:
  push:
    branches:
      - feature/minimal-validation
    paths:
      - 'main.tf'
      - 'variables.tf'
      - 'outputs.tf'
      - 'providers.tf'
      - 'env/validation.tfvars'
      - '.github/workflows/terraform-validate-minimal.yml'
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      action:
        description: '选择操作 (plan/apply/destroy)'
        required: true
        default: 'plan'

env:
  TERRAFORM_VERSION: '1.6.0'

jobs:
  validate:
    name: Terraform Validate
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

  plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    needs: validate
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Extract Azure Subscription ID
        run: |
          SUBSCRIPTION_ID=$(az account show --query 'id' -o tsv)
          echo "SUBSCRIPTION_ID=${SUBSCRIPTION_ID}" >> $GITHUB_ENV
          echo "✅ Azure CLI 认证成功，订阅 ID: ${SUBSCRIPTION_ID}"

      - name: Generate Dynamic DNS Subdomain
        run: |
          TIMESTAMP=$(date +%s)
          echo "DNS_SUBDOMAIN=${TIMESTAMP}" >> $GITHUB_ENV
          echo "DNS_RECORD_VALUE=${SUBSCRIPTION_ID}" >> $GITHUB_ENV
          echo "✅ 生成动态 DNS 配置"
          echo "   - Subdomain (timestamp): ${TIMESTAMP}"
          echo "   - Record Value (subscription ID): ${SUBSCRIPTION_ID}"

      - name: Terraform Init
        env:
          TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          TF_VAR_tencent_secret_id: ${{ secrets.TENCENT_SECRET_ID }}
          TF_VAR_tencent_secret_key: ${{ secrets.TENCENT_SECRET_KEY }}
        run: terraform init

      - name: Debug - Check Tencent Cloud Secrets
        env:
          TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          TF_VAR_tencent_secret_id: ${{ secrets.TENCENT_SECRET_ID }}
          TF_VAR_tencent_secret_key: ${{ secrets.TENCENT_SECRET_KEY }}
        run: |
          if [ -z "$TENCENTCLOUD_SECRET_ID" ] || [ -z "$TENCENTCLOUD_SECRET_KEY" ]; then
            echo "❌ ERROR: Tencent Cloud secrets are not configured!"
            echo "   - TENCENT_SECRET_ID: $([ -z "$TENCENTCLOUD_SECRET_ID" ] && echo 'NOT SET' || echo '✅ SET')"
            echo "   - TENCENT_SECRET_KEY: $([ -z "$TENCENTCLOUD_SECRET_KEY" ] && echo 'NOT SET' || echo '✅ SET')"
            exit 1
          else
            echo "✅ Tencent Cloud secrets are properly configured"
            echo "   - Secret ID length: ${#TENCENTCLOUD_SECRET_ID}"
            echo "   - Secret Key length: ${#TENCENTCLOUD_SECRET_KEY}"
            echo "   - TF_VAR_tencent_secret_id length: ${#TF_VAR_tencent_secret_id}"
            echo "   - TF_VAR_tencent_secret_key length: ${#TF_VAR_tencent_secret_key}"
          fi

      - name: Terraform Plan - DNS Verification
        env:
          TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          TF_VAR_tencent_secret_id: ${{ secrets.TENCENT_SECRET_ID }}
          TF_VAR_tencent_secret_key: ${{ secrets.TENCENT_SECRET_KEY }}
        run: |
          terraform plan \
            -var-file="env/validation.tfvars" \
            -var="dns_subdomain=${DNS_SUBDOMAIN}" \
            -out=tfplan \
            -lock=false

      - name: Upload Plan Artifact
        uses: actions/upload-artifact@v4
        with:
          name: tfplan
          path: tfplan
          retention-days: 5

  apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: plan
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Extract Azure Subscription ID
        run: |
          SUBSCRIPTION_ID=$(az account show --query 'id' -o tsv)
          echo "SUBSCRIPTION_ID=${SUBSCRIPTION_ID}" >> $GITHUB_ENV
          echo "✅ Azure CLI 认证成功，订阅 ID: ${SUBSCRIPTION_ID}"

      - name: Generate Dynamic DNS Subdomain
        run: |
          TIMESTAMP=$(date +%s)
          echo "DNS_SUBDOMAIN=${TIMESTAMP}" >> $GITHUB_ENV
          echo "DNS_RECORD_VALUE=${SUBSCRIPTION_ID}" >> $GITHUB_ENV
          echo "✅ 生成动态 DNS 配置"
          echo "   - Subdomain (timestamp): ${TIMESTAMP}"
          echo "   - Record Value (subscription ID): ${SUBSCRIPTION_ID}"

      - name: Terraform Init
        env:
          TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          TF_VAR_tencent_secret_id: ${{ secrets.TENCENT_SECRET_ID }}
          TF_VAR_tencent_secret_key: ${{ secrets.TENCENT_SECRET_KEY }}
        run: terraform init

      - name: Debug - Check Tencent Cloud Secrets
        env:
          TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          TF_VAR_tencent_secret_id: ${{ secrets.TENCENT_SECRET_ID }}
          TF_VAR_tencent_secret_key: ${{ secrets.TENCENT_SECRET_KEY }}
        run: |
          if [ -z "$TENCENTCLOUD_SECRET_ID" ] || [ -z "$TENCENTCLOUD_SECRET_KEY" ]; then
            echo "❌ ERROR: Tencent Cloud secrets are not configured!"
            echo "   - TENCENT_SECRET_ID: $([ -z "$TENCENTCLOUD_SECRET_ID" ] && echo 'NOT SET' || echo '✅ SET')"
            echo "   - TENCENT_SECRET_KEY: $([ -z "$TENCENTCLOUD_SECRET_KEY" ] && echo 'NOT SET' || echo '✅ SET')"
            exit 1
          else
            echo "✅ Tencent Cloud secrets are properly configured"
            echo "   - Secret ID length: ${#TENCENTCLOUD_SECRET_ID}"
            echo "   - Secret Key length: ${#TENCENTCLOUD_SECRET_KEY}"
            echo "   - TF_VAR_tencent_secret_id length: ${#TF_VAR_tencent_secret_id}"
            echo "   - TF_VAR_tencent_secret_key length: ${#TF_VAR_tencent_secret_key}"
          fi

      - name: Download Plan Artifact
        uses: actions/download-artifact@v4
        with:
          name: tfplan

      - name: Terraform Apply - DNS Verification
        env:
          TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          TF_VAR_tencent_secret_id: ${{ secrets.TENCENT_SECRET_ID }}
          TF_VAR_tencent_secret_key: ${{ secrets.TENCENT_SECRET_KEY }}
        run: |
          terraform apply -auto-approve tfplan

      - name: Show Outputs
        run: terraform output -json

  destroy:
    name: Terraform Destroy
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'destroy'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Extract Azure Subscription ID
        run: |
          SUBSCRIPTION_ID=$(az account show --query 'id' -o tsv)
          echo "SUBSCRIPTION_ID=${SUBSCRIPTION_ID}" >> $GITHUB_ENV
          echo "✅ Azure CLI 认证成功，订阅 ID: ${SUBSCRIPTION_ID}"

      - name: Generate Dynamic DNS Subdomain
        run: |
          TIMESTAMP=$(date +%s)
          echo "DNS_SUBDOMAIN=${TIMESTAMP}" >> $GITHUB_ENV
          echo "DNS_RECORD_VALUE=${SUBSCRIPTION_ID}" >> $GITHUB_ENV
          echo "✅ 生成动态 DNS 配置"
          echo "   - Subdomain (timestamp): ${TIMESTAMP}"
          echo "   - Record Value (subscription ID): ${SUBSCRIPTION_ID}"

      - name: Terraform Init
        env:
          TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          TF_VAR_tencent_secret_id: ${{ secrets.TENCENT_SECRET_ID }}
          TF_VAR_tencent_secret_key: ${{ secrets.TENCENT_SECRET_KEY }}
        run: terraform init

      - name: Debug - Check Tencent Cloud Secrets
        env:
          TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          TF_VAR_tencent_secret_id: ${{ secrets.TENCENT_SECRET_ID }}
          TF_VAR_tencent_secret_key: ${{ secrets.TENCENT_SECRET_KEY }}
        run: |
          if [ -z "$TENCENTCLOUD_SECRET_ID" ] || [ -z "$TENCENTCLOUD_SECRET_KEY" ]; then
            echo "❌ ERROR: Tencent Cloud secrets are not configured!"
            echo "   - TENCENT_SECRET_ID: $([ -z "$TENCENTCLOUD_SECRET_ID" ] && echo 'NOT SET' || echo '✅ SET')"
            echo "   - TENCENT_SECRET_KEY: $([ -z "$TENCENTCLOUD_SECRET_KEY" ] && echo 'NOT SET' || echo '✅ SET')"
            exit 1
          else
            echo "✅ Tencent Cloud secrets are properly configured"
            echo "   - Secret ID length: ${#TENCENTCLOUD_SECRET_ID}"
            echo "   - Secret Key length: ${#TENCENTCLOUD_SECRET_KEY}"
            echo "   - TF_VAR_tencent_secret_id length: ${#TF_VAR_tencent_secret_id}"
            echo "   - TF_VAR_tencent_secret_key length: ${#TF_VAR_tencent_secret_key}"
          fi

      - name: Terraform Destroy - DNS Verification
        env:
          TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          TF_VAR_tencent_secret_id: ${{ secrets.TENCENT_SECRET_ID }}
          TF_VAR_tencent_secret_key: ${{ secrets.TENCENT_SECRET_KEY }}
        run: |
          terraform destroy \
            -var-file="env/validation.tfvars" \
            -var="dns_subdomain=${DNS_SUBDOMAIN}" \
            -auto-approve \
            -lock=false
          echo "✅ DNS 测试记录已删除"
